#include <stdio.h>

void MyEnum();

int main()
{
	MyEnum();

	return 0;
}

void MyEnum()
{
	{
		enum color
		{
			Red = 0,
			Orange = 1,
			Yellow = 2
		};

		int a = 0;

		if (a == Red)
			printf("RED\n");

		for (a = Red; a <= Yellow; a++)
		{
			printf("%d \n", a);
		}
	}
}
